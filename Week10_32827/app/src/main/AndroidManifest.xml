<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="umn.ac.id.week10_32827">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Week10_32827">
        <service
            android:name=".CustomBoundService"
            android:enabled="true"
            android:exported="true"></service>
        <service
            android:name=".CustomService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".CustomService"
            android:enabled="true"
            android:exported="true" />
        <service android:name=".SimpleIntentService" />

        <activity
            android:name=".Project3"
            android:label="Project 3"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Project2"
            android:label="Project 2"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Project1"
            android:label="Project 1"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>